/**
 * Playground script for manual ORM experiments.
 *
 * - Point PLAYGROUND_PAGE_ID at a dedicated sandbox page in Notion.
 * - Feel free to edit this file locally while testing, then reset it afterwards.
 */
import "dotenv/config";

async function main() {
  //const parentId = process.env.PLAYGROUND_PAGE_ID;
}

main();
