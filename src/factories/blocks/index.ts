/**
 * Block factory barrel export
 * Organized by category: text, media, layout, and advanced blocks
 */

// Text blocks (paragraph, headings, lists, quotes, toggles, to-dos)
export * from "./text";

// Media blocks (image, video, embed, bookmark, PDF, file, audio)
export * from "./media";

// Layout blocks (divider, table, columns, breadcrumb, table of contents)
export * from "./layout";

// Advanced blocks (code, callout, equation, synced blocks, link-to-page, template)
export * from "./advanced";
